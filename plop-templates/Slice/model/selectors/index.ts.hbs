import { RootState } from "@/app/providers/StoreProvider";

export const data = (state: RootState) => state.{{layer}}{{slice}}.data;
export const isLoading = (state: RootState) => state.{{layer}}{{slice}}.isLoading;
export const error = (state: RootState) => state.{{layer}}{{slice}}.error;
